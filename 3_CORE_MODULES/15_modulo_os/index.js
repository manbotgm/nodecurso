const os = require("os");

console.log(os.cpus());

console.log(os.freemem()); //memoria ran livre

console.log(os.homedir());  //diretorio principal da maquina, onde esta o HOME

console.log(os.type());